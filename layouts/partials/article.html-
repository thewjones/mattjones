<article>
  <header>
    <h1>{{ .Title }} part</h1>
    <ul>
      <li>Posted by <strong>Matt</strong> (<a href="{{ .Permalink }}">Permalink</a>)</li>
      <li>Published on <time>{{ .PublishDate.Format "2 January 2006" }}</time></li>
        </ul>
  </header>
  
  {{ if isset .Params "resources" }}
  {{ if $header := (.Resources.ByType "image").GetMatch "header" }}


  {{ $filter := images.Process "resize 1000x q75 webp" }}

  {{if $header}}
  
  <figure>
    {{ with . | images.Filter $filter }}
    <img src="{{ $header.RelPermalink }}" loading="lazy">
    {{ end }}
    <figcaption >
      <span id="caption">
      {{if .Params.imageCaption }}
      {{ .Params.imageCaption }}
      {{ end }}
      </span>
      {{ if .Params.imageLicense }}
      <span id="credit">
        Credit: <a href="{{ .Params.imageURL }}">{{ .Params.imageAttrib }}</a> &middot;
        <span id="license">
          <a href="{{ .Params.licenseURL }}">
            {{ .Params.imageLicense }}
          </a>
        </span>
      </span>
      {{ end }}
      
    </figcaption>
  </figure>
  
  {{ end }}
  {{ end }}
  {{ end }}
 
  {{ .Content }}
  
  <footer>
  
  </footer>
</article>
